<h1> Choose your teleporter </h1>

<table id="teleporters">
	<thead>
		<tr>
			<th id="teleport_number_header"> From </th>
			<th id="from_header"> From </th>
			<th id="to_header"> To </th>
			<th id="sit_remaining_header"> Sit remaining </th>
			<th> More info </th>
		</tr>
	</thead>
	<tbody>
		<% @teleporters.each do |teleporter| %>
    <tr class="<%= (teleporter.full?)? 'full':'free' %>">
			<td><%= teleporter.number %></td>
      <td><%= teleporter.departure %></td>
      <td><%= teleporter.destination %></td>
			<td><%= teleporter.count_remaining_seat %></td>
			<td disabled='<%= !teleporter.full? %>'><%= link_to 'More info', '/'%></td>
    </tr>
  	<% end %>
	</tbody>
</table>

{params[:order] == "release_date" ? {:class => "hilite"} : {}}
